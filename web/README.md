# Tally — Angular application

The app itself. See the repository root for everything else:

- [`../README.md`](../README.md) — what Tally does, and how it is built.
- [`../CONTRIBUTING.md`](../CONTRIBUTING.md) — setup, commands, and conventions.
- [`../docs/`](../docs) — the specification it implements.

```bash
npm install
npm start                                            # dev server, :4200
npm run build
npx ng test --watch=false --browsers=ChromeHeadless
```

There is no e2e suite.
